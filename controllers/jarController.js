const db = require("../models/jarDatabase");

// Defining methods for the Controller


/* module.exports = {
  findById: function(req, res) {
    db.Jar
      .findById(req.params.id)
      .then(dbModel => res.json(dbModel))
      .catch(err => res.status(422).json(err));
  },
  create: function(req, res) {
    db.Jar
      .create(req.body)
      .then(dbModel => res.json(dbModel))
      .catch(err => res.status(422).json(err));
  },
  update: function(req, res) {
    db.Jar
      .findOneAndUpdate({ _id: req.params.id }, req.body)
      .then(dbModel => res.json(dbModel))
      .catch(err => res.status(422).json(err));
  }
}; */

// module.exports = {
// 	ctrl.addpaperclip = function (req, res) {
// 		db.Jars.findOutAndUpdate(req.body.id, paperclipsIn)
// 			.then() {
// 			res.json(dbResponse)
// 		})
// 	.catch() = {
// 			res.status().json(err.response)
// 		})
// })
// });

